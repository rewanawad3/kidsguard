import 'package:flutter/material.dart';

// Colors from your design
const Color kPrimaryColor = Color(0xFF1977BD); // login / main button
const Color kLightBlue = Color(0xFF8EACCD); // top background shape
const Color kTextColor = Color(0xFF365D81); // main font color

// Font family - must match the font family in pubspec.yaml
const String kFontLexend = 'Lexend';

// Title style used on Create Account title
const TextStyle kTitleStyle = TextStyle(
  fontFamily: kFontLexend,
  fontSize: 28,
  fontWeight: FontWeight.w600,
  color: kTextColor,
);
